- name: installing nginx and running
  hosts: web
  become: yes
  tasks:
  - name: installing nginx 
    ansible.builtin.package:
     name: nginx
     state: present

  - name: installing nginx 
    ansible.builtin.package:
     name: nginx
     state: present   
